# Execute native hillslope water balance and batch receipt contention package

Execute
`docs/work-packages/20260908-hillslope-watbal-native-001/package.md` end to end
on `forest` across `/workdir/wepppyo3` and `/workdir/wepppy`.

Read both repositories' root and applicable nested `AGENTS.md` files before
editing. Read the complete active ExecPlan and keep it, `tracker.md`, and all
required evidence current while working. Treat this as faithful extraction of
the current hillslope-water-balance contract, not a surrogate implementation or
an opportunity to change formulas.

Reuse the existing Git LFS single-OFE, `incommensurate-stickball`, and
`throbbing-sylvan` fixtures plus the prior deterministic scale harness. Verify
their hashes before use. Treat `/wc1` as read-only, never write into the active
production batch tree, and use only disposable outputs for benchmarks.

Capture current Python output before deleting the producer. Require the native
API at runtime with no Python/PyArrow/pandas full-source fallback. Do not claim
completion merely because Rust compiles: acceptance requires exact generated
summary/cache/report evidence and the sequential native totalwatsed3 followed
by native hillslope watbal path in a real 12 GiB Forest Compose worker.

Repair the finalizer job-ID stale write through fresh durable hydration under
the NoDb lock. Preserve the compatibility mirror, unrelated concurrent state,
root RQ metadata, failure-tolerant dependencies, Omni linkage, and dashboard
behavior. Never retry a stale object's dump, weaken `NoDbStaleWriteError`, clear
global caches, or suppress persistence failure.

Complete independent correctness, QA, and security reviews. Resolve every
medium/high finding, run the focused and broad validation gates, commit and push
wepppyo3 before WEPPpy, and verify both remotes and LFS objects from clean Forest
checkouts. Registry publication is permitted only if separately authorized;
openwepp.org deployment is outside this package.
