# Execute native totalwatsed3 producer package

Execute
`docs/work-packages/20260907-totalwatsed3-native-producer-001/package.md`
end to end on `forest` across `/workdir/wepppyo3` and `/workdir/wepppy`.

Read both repositories' root and applicable nested `AGENTS.md` files before
editing. Treat `/wc1/runs/in/incommensurate-stickball` and
`/wc1/runs/th/throbbing-sylvan` as read-only HPC source data. Copy only the
specified Parquet files, verify every recorded checksum, and perform all writes
against repository fixtures or disposable benchmark directories.

Maintain `tracker.md` and every required evidence artifact as work progresses.
Use explicit `Static:` and `Ran:` sections and the evidence labels required by
`docs/claim-discipline.md`. Capture the Python oracle before deleting its
producer. Do not retain a Python/DuckDB/pandas fallback. Do not weaken parity,
memory, wall-time, LFS, release-provenance, security, or Forest Compose gates.

Complete both repositories, commit and push them in the package's required
order, and verify the LFS fixtures from a clean Forest checkout. Stop only for
a package stop condition or a genuine authority/external dependency blocker.
