# Evidence artifacts

The executing agent creates and maintains the artifacts listed in
`../tracker.md`. Every artifact must distinguish `Static:` inspection from
`Ran:` command evidence and label claims as `confirmed`, `inference`, or
`hypothesis` according to `../../../claim-discipline.md`.

Do not copy secrets, credentials, or irrelevant run metadata from HPC fixtures.
